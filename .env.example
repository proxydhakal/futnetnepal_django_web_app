SECRET_KEY=change-me
DEBUG=True
USE_SQLITE=True

# Database (PostgreSQL when USE_SQLITE=False)
DB_NAME=futnetnepal
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Static & media (paths relative to project root unless absolute)
STATIC_URL=/static/
STATIC_ROOT=staticfiles
MEDIA_URL=/media/
MEDIA_ROOT=media

# Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

SITE_URL=http://127.0.0.1:8000

# Redis (Channels / WebSockets)
REDIS_URL=redis://127.0.0.1:6379/0
